<%= @user.username %>, pupugi deigns to inform you...
=====================================================

<% event_time = Time.zone.parse(@event.date_time.to_s) %>

The <%= @event.game_title %> event:

<%= @event.session_title %>

is in two days, on <%= event_time.strftime("%A, the #{event_time.day.ordinalize} of %b, %Y") %>,

and starts at <%= event_time.strftime('%l:%M %p') %>.

Go here to view the event: <%= @url %>